-- DuckDB-compatible schema for siscob_teste
-- Adapted from mysqlreptester schema.go
-- Removed: AUTO_INCREMENT, FOREIGN KEYs, secondary indexes,
--          ON UPDATE CURRENT_TIMESTAMP, DEFAULT CHARSET

CREATE DATABASE IF NOT EXISTS siscob_teste;
USE siscob_teste;

CREATE TABLE IF NOT EXISTS EMPRESA (
    ID_EMPRESA INT NOT NULL,
    NOME VARCHAR(40) NOT NULL,
    NOME_ATUAL VARCHAR(40) NOT NULL,
    PRIMARY KEY (ID_EMPRESA)
) ENGINE=DUCKDB;

CREATE TABLE IF NOT EXISTS CARTEIRA (
    ID_CARTEIRA INT NOT NULL,
    NUM_GRUPO INT NOT NULL DEFAULT 0,
    ID_EMPRESA INT NOT NULL DEFAULT 0,
    ID_CONTA INT NOT NULL DEFAULT 0,
    NOME VARCHAR(60) DEFAULT NULL,
    STATUS INT DEFAULT 1,
    TIPO_BEM TINYINT NOT NULL DEFAULT 0,
    TIPO_CREDITO TINYINT UNSIGNED NOT NULL DEFAULT 0,
    ID_FIRMA INT NOT NULL DEFAULT 0,
    ID_FILIAL INT NOT NULL DEFAULT 0,
    PRACA INT NOT NULL DEFAULT 0,
    FICHAID_DIF_CONTR TINYINT NOT NULL DEFAULT 0,
    EXCLUI_MALA TINYINT UNSIGNED NOT NULL DEFAULT 0,
    PRIMARY KEY (ID_CARTEIRA)
) ENGINE=DUCKDB;

CREATE TABLE IF NOT EXISTS FUNCIONARIO (
    ID_FUNCIONARIO INT NOT NULL,
    ID_FILIAL INT NOT NULL DEFAULT 0,
    ID_TURNO INT NOT NULL DEFAULT 0,
    ID_CONTR INT NOT NULL DEFAULT 0,
    LOGIN VARCHAR(20) NOT NULL DEFAULT '',
    NOME VARCHAR(100) DEFAULT NULL,
    NOME_SOCIAL VARCHAR(100) DEFAULT NULL,
    STATUS TINYINT UNSIGNED DEFAULT 1,
    SENHA VARCHAR(100) DEFAULT NULL,
    SENHA_ANT VARCHAR(100) DEFAULT NULL,
    SENHA_KEY VARCHAR(100) DEFAULT NULL,
    SENHA_EMAIL VARCHAR(100) DEFAULT NULL,
    FUNCAO TINYINT UNSIGNED DEFAULT 0,
    ACL_F VARCHAR(500) DEFAULT NULL,
    ID_SUP INT DEFAULT NULL,
    DATA_SAIDA DATE DEFAULT NULL,
    EMP_PESQUISA VARCHAR(75) DEFAULT NULL,
    TIPO_PESQUISA TINYINT NOT NULL DEFAULT 0,
    DATA_CONTRATACAO DATE DEFAULT NULL,
    DATA_EXPIRACAO DATE DEFAULT NULL,
    ID_EMPRESA INT NOT NULL DEFAULT 0,
    WEB TINYINT UNSIGNED NOT NULL DEFAULT 0,
    CPF_F VARCHAR(15) DEFAULT '',
    ERRO_LOGIN TINYINT UNSIGNED DEFAULT 0,
    EMAIL VARCHAR(50) DEFAULT NULL,
    RE VARCHAR(20) DEFAULT NULL,
    INFO_ADICIONAL VARCHAR(20) DEFAULT NULL,
    HABILITA_UPDATE TINYINT UNSIGNED DEFAULT 0,
    ATIVO TINYINT NOT NULL DEFAULT 0,
    LOG_SQL TINYINT UNSIGNED DEFAULT 0,
    TELA_MAXIMIZADA TINYINT NOT NULL DEFAULT 0,
    DATA_NASC DATE DEFAULT NULL,
    REGIME_TRABALHO TINYINT NOT NULL DEFAULT 0,
    MODALIDADE_TRABALHO TINYINT NOT NULL DEFAULT 0,
    LOGIN_REDE VARCHAR(256) NOT NULL DEFAULT '',
    DX_INSTANCIA TINYINT DEFAULT 0,
    DATA_ALT DATETIME DEFAULT NULL,
    PRIMARY KEY (ID_FUNCIONARIO)
) ENGINE=DUCKDB;

CREATE TABLE IF NOT EXISTS CONTRATO (
    ID_CONTR INT NOT NULL,
    ID_FUNC_TEMP INT NOT NULL DEFAULT 0,
    ID_FUNCIONARIO INT NOT NULL DEFAULT 0,
    NUM_CREDOR TINYINT UNSIGNED NOT NULL DEFAULT 0,
    ID_CARTEIRA INT NOT NULL DEFAULT 1,
    ID_FILIAL INT NOT NULL DEFAULT 0,
    ID_FUNC_AC INT NOT NULL DEFAULT 0,
    ESCRITORIO_DEST INT NOT NULL DEFAULT 0,
    ESCRITORIO_ORIG INT NOT NULL DEFAULT 0,
    ID_MALA INT NOT NULL DEFAULT 0,
    ID_END INT NOT NULL DEFAULT 0,
    ID_TEL INT NOT NULL DEFAULT 0,
    ID_EMAIL INT NOT NULL DEFAULT 0,
    ID_CENTRO_CUSTO INT NOT NULL DEFAULT 0,
    NUM_CONTRATO VARCHAR(20) DEFAULT NULL,
    FASE INT NOT NULL DEFAULT 0,
    FICHA_ID VARCHAR(20) DEFAULT NULL,
    STATUS TINYINT UNSIGNED DEFAULT 0,
    BLOQUEIO TINYINT UNSIGNED NOT NULL DEFAULT 0,
    NOME VARCHAR(50) DEFAULT NULL,
    CPF VARCHAR(15) DEFAULT NULL,
    RG VARCHAR(18) DEFAULT NULL,
    PESSOA_JURIDICA TINYINT NOT NULL DEFAULT 0,
    SEXO CHAR(1) DEFAULT NULL,
    DATA_NASC DATE DEFAULT NULL,
    NACIONALIDADE VARCHAR(20) DEFAULT NULL,
    ESTADO_CIVIL VARCHAR(15) DEFAULT NULL,
    CONJUGE VARCHAR(40) DEFAULT NULL,
    NOME_PAI VARCHAR(40) DEFAULT NULL,
    NOME_MAE VARCHAR(40) DEFAULT NULL,
    LOCAL_TRABALHO VARCHAR(31) DEFAULT NULL,
    PRINCIPAL DECIMAL(10,2) DEFAULT 0.00,
    PRINCIPAL_ATUAL DECIMAL(10,2) DEFAULT 0.00,
    PRIM_VENC_ORIGINAL DATE DEFAULT NULL,
    DATA_CARGA DATETIME DEFAULT NULL,
    DATA_SAIDA DATE DEFAULT NULL,
    ULTIMA_FC INT DEFAULT 0,
    ULT_HIST_COB INT NOT NULL DEFAULT 0,
    ULT_DT_COB DATETIME DEFAULT NULL,
    SEQ_COB SMALLINT UNSIGNED DEFAULT NULL,
    ULT_HIST INT NOT NULL DEFAULT 0,
    ULT_DT_HIST DATETIME DEFAULT NULL,
    SEQ_HIST SMALLINT UNSIGNED DEFAULT NULL,
    VAL_TRANSF DATE DEFAULT NULL,
    QTDE_AGENDA INT NOT NULL DEFAULT 0,
    QTDE_PESQUISA INT NOT NULL DEFAULT 0,
    QTDE_PROMESSA INT NOT NULL DEFAULT 0,
    QTDE_REEMISSAO_FC INT DEFAULT 0,
    DATA_SAIDA_EFET DATE DEFAULT NULL,
    TIPO TINYINT UNSIGNED NOT NULL DEFAULT 0,
    ORIGEM TINYINT NOT NULL DEFAULT 0,
    STATUS_JUR TINYINT UNSIGNED NOT NULL DEFAULT 0,
    SCORE INT NOT NULL DEFAULT -1,
    SCORE_INTERNO INT NOT NULL DEFAULT -1,
    TIPO_DEVOLUCAO TINYINT UNSIGNED NOT NULL DEFAULT 0,
    DATA_IMPORTACAO DATETIME DEFAULT NULL,
    CODIGO VARCHAR(50) DEFAULT NULL,
    MOTIVO_INADIMPLENCIA INT NOT NULL DEFAULT 0,
    MOTIVO_RETORNO TINYINT DEFAULT NULL,
    DEVOLVIDO TINYINT UNSIGNED DEFAULT 0,
    DATA_ESCRITORIO_DEST DATETIME DEFAULT NULL,
    DATA_ESCRITORIO_ORIG DATETIME DEFAULT NULL,
    DATA_CAD DATETIME DEFAULT NULL,
    CELULA_COBRANCA INT NOT NULL DEFAULT 0,
    MOTIVO_BLOQUEIO VARCHAR(255) DEFAULT NULL,
    DATA_ALT DATETIME DEFAULT NULL,
    PRIMARY KEY (ID_CONTR)
) ENGINE=DUCKDB;

CREATE TABLE IF NOT EXISTS TELEFONE (
    ID_TEL INT NOT NULL,
    CPF VARCHAR(15) DEFAULT NULL,
    DDD INT DEFAULT 0,
    NUMERO VARCHAR(11) NOT NULL DEFAULT '0',
    RAMAL INT DEFAULT 0,
    STATUS TINYINT UNSIGNED DEFAULT 1,
    TIPO TINYINT UNSIGNED NOT NULL DEFAULT 0,
    CONTATO TINYINT UNSIGNED DEFAULT NULL,
    NOME_CONTATO VARCHAR(20) DEFAULT NULL,
    ORIGEM TINYINT UNSIGNED DEFAULT 0,
    ULTIMA_ATUALIZ DATETIME DEFAULT NULL,
    ID_FUNCIONARIO INT NOT NULL DEFAULT 0,
    DATA_CRIACAO DATETIME DEFAULT NULL,
    OBSERVACAO VARCHAR(20) DEFAULT NULL,
    QTDE_CONTATO INT DEFAULT 0,
    QTDE_CONTATO_POS INT DEFAULT 0,
    SCORE INT DEFAULT 0,
    DATA_ALT DATETIME DEFAULT NULL,
    PRIMARY KEY (ID_TEL)
) ENGINE=DUCKDB;

CREATE TABLE IF NOT EXISTS ENDERECO (
    ID_END INT NOT NULL,
    CPF VARCHAR(15) DEFAULT NULL,
    RUA VARCHAR(100) DEFAULT NULL,
    NUMERO VARCHAR(10) DEFAULT NULL,
    COMPL VARCHAR(100) DEFAULT NULL,
    BAIRRO VARCHAR(100) DEFAULT NULL,
    CIDADE VARCHAR(100) DEFAULT NULL,
    ESTADO CHAR(2) DEFAULT NULL,
    CEP VARCHAR(9) DEFAULT NULL,
    STATUS TINYINT UNSIGNED DEFAULT 1,
    TIPO TINYINT UNSIGNED NOT NULL DEFAULT 0,
    CONTATO TINYINT UNSIGNED DEFAULT NULL,
    ORIGEM TINYINT UNSIGNED DEFAULT 0,
    ULTIMA_ATUALIZ DATETIME DEFAULT NULL,
    ID_FUNCIONARIO INT NOT NULL DEFAULT 0,
    OBSERVACAO VARCHAR(20) DEFAULT NULL,
    DATA_CRIACAO DATETIME DEFAULT NULL,
    SCORE INT NOT NULL DEFAULT 0,
    DATA_ALT DATETIME DEFAULT NULL,
    PRIMARY KEY (ID_END)
) ENGINE=DUCKDB;

CREATE TABLE IF NOT EXISTS EMAIL (
    ID_EMAIL INT NOT NULL,
    CPF VARCHAR(15) NOT NULL,
    ID_FUNCIONARIO INT NOT NULL DEFAULT 0,
    EMAIL VARCHAR(100) DEFAULT NULL,
    STATUS TINYINT UNSIGNED DEFAULT 1,
    ULTIMA_ATUALIZ DATETIME DEFAULT NULL,
    ORIGEM TINYINT UNSIGNED DEFAULT 0,
    TIPO TINYINT UNSIGNED DEFAULT 0,
    DATA_CRIACAO DATETIME DEFAULT NULL,
    DATA_ALT DATETIME DEFAULT NULL,
    OBSERVACAO VARCHAR(20) DEFAULT NULL,
    SCORE INT DEFAULT 0,
    PRIMARY KEY (ID_EMAIL)
) ENGINE=DUCKDB;

CREATE TABLE IF NOT EXISTS ACORDO (
    ID_ACORDO INT NOT NULL,
    ID_FUNCIONARIO INT NOT NULL DEFAULT 0,
    ID_FUNC_CONTR INT NOT NULL DEFAULT 0,
    ID_CONTR INT NOT NULL DEFAULT 0,
    ID_CARTEIRA INT NOT NULL DEFAULT 0,
    STATUS TINYINT UNSIGNED NOT NULL DEFAULT 0,
    DATA DATETIME DEFAULT NULL,
    TIPO TINYINT UNSIGNED NOT NULL DEFAULT 0,
    TIPO_ACORDO TINYINT NOT NULL DEFAULT 0,
    PRINCIPAL DECIMAL(10,2) DEFAULT 0.00,
    MULTA DECIMAL(10,2) DEFAULT 0.00,
    JUROS DECIMAL(10,2) DEFAULT 0.00,
    CORRECAO DECIMAL(10,2) DEFAULT 0.00,
    NOTIFICACAO DECIMAL(10,2) DEFAULT 0.00,
    HONORARIOS DECIMAL(10,2) DEFAULT 0.00,
    TOTAL DECIMAL(10,2) DEFAULT 0.00,
    CHEQUE_DEV TINYINT UNSIGNED DEFAULT 0,
    NUM_PARCELAS INT DEFAULT NULL,
    QTDE_CONTR INT DEFAULT NULL,
    NUM_PARC_ABERTO DECIMAL(10,3) DEFAULT NULL,
    VENC_PARC_ABERTO DATE DEFAULT NULL,
    FP_PARC_PROX TINYINT UNSIGNED NOT NULL DEFAULT 0,
    DATA_CAPA DATETIME DEFAULT NULL,
    DESPESA DECIMAL(10,2) DEFAULT 0.00,
    DT_GRAVACAO DATETIME DEFAULT NULL,
    DATA_CANC DATETIME DEFAULT NULL,
    DATA_REVERTE DATETIME DEFAULT NULL,
    AJUIZADO TINYINT NOT NULL DEFAULT 0,
    BLOQUEIO_FC TINYINT UNSIGNED NOT NULL DEFAULT 0,
    STATUS_JUR_AC TINYINT NOT NULL DEFAULT 0,
    EXCECAO TINYINT DEFAULT 0,
    FASE VARCHAR(50) DEFAULT NULL,
    CELULA_COBRANCA INT NOT NULL DEFAULT 0,
    ENVIO_PROPOSTA TINYINT NOT NULL DEFAULT 0,
    NOME_CAMPANHA VARCHAR(50) DEFAULT NULL,
    ID_CENTRO_CUSTO INT NOT NULL DEFAULT 0,
    FORMA_PGTO_PREF INT DEFAULT 0,
    PREVENTIVO TINYINT UNSIGNED DEFAULT NULL,
    DATA_ALT DATETIME DEFAULT NULL,
    NUM_PARC_PRIM DECIMAL(10,3) DEFAULT 1.000,
    PRIMARY KEY (ID_ACORDO)
) ENGINE=DUCKDB;

CREATE TABLE IF NOT EXISTS PARCELA (
    ID_ACORDO INT NOT NULL DEFAULT 0,
    NUM_PARC DECIMAL(10,3) NOT NULL DEFAULT 0.000,
    ID_RECIBO INT NOT NULL DEFAULT 0,
    ID_FC INT NOT NULL DEFAULT 0,
    PRINCIPAL DECIMAL(10,2) NOT NULL DEFAULT 0.00,
    MULTA DECIMAL(10,2) NOT NULL DEFAULT 0.00,
    JUROS DECIMAL(10,2) NOT NULL DEFAULT 0.00,
    CORRECAO DECIMAL(10,2) NOT NULL DEFAULT 0.00,
    HONORARIOS DECIMAL(10,2) NOT NULL DEFAULT 0.00,
    NOTIFICACAO DECIMAL(10,2) NOT NULL DEFAULT 0.00,
    TOTAL DECIMAL(10,2) NOT NULL DEFAULT 0.00,
    VENCIMENTO DATE DEFAULT NULL,
    FORMA_PGTO TINYINT UNSIGNED DEFAULT 0,
    STATUS TINYINT UNSIGNED DEFAULT 0,
    QTDE_CHEQUES INT DEFAULT 0,
    ATRASO INT DEFAULT 0,
    VENC_ORIGINAL DATE DEFAULT NULL,
    DATA_BAIXA DATE DEFAULT NULL,
    DATA_PGTO DATE DEFAULT NULL,
    DATA_PC DATE DEFAULT NULL,
    REPASSE DECIMAL(10,2) NOT NULL DEFAULT 0.00,
    COMISSAO_EMPR DECIMAL(10,2) NOT NULL DEFAULT 0.00,
    COMISSAO_COB DECIMAL(10,2) NOT NULL DEFAULT 0.00,
    PC TINYINT NOT NULL DEFAULT 0,
    DESPESA DECIMAL(10,2) NOT NULL DEFAULT 0.00,
    VALOR_PGTO DECIMAL(10,2) NOT NULL DEFAULT 0.00,
    PRINCIPAL_C DECIMAL(10,2) NOT NULL DEFAULT 0.00,
    MULTA_C DECIMAL(10,2) NOT NULL DEFAULT 0.00,
    JUROS_C DECIMAL(10,2) NOT NULL DEFAULT 0.00,
    CORRECAO_C DECIMAL(10,2) NOT NULL DEFAULT 0.00,
    HONORARIOS_C DECIMAL(10,2) NOT NULL DEFAULT 0.00,
    NOTIFICACAO_C DECIMAL(10,2) NOT NULL DEFAULT 0.00,
    PRINCIPAL_O DECIMAL(10,2) NOT NULL DEFAULT 0.00,
    MULTA_O DECIMAL(10,2) NOT NULL DEFAULT 0.00,
    JUROS_O DECIMAL(10,2) NOT NULL DEFAULT 0.00,
    CORRECAO_O DECIMAL(10,2) NOT NULL DEFAULT 0.00,
    HONORARIOS_O DECIMAL(10,2) NOT NULL DEFAULT 0.00,
    NOTIFICACAO_O DECIMAL(10,2) NOT NULL DEFAULT 0.00,
    NUM_REC_INTERNO INT NOT NULL DEFAULT 0,
    DATA_CREDITO DATE DEFAULT NULL,
    DATA_ALT DATETIME DEFAULT NULL,
    PRIMARY KEY (ID_ACORDO, NUM_PARC)
) ENGINE=DUCKDB;

CREATE TABLE IF NOT EXISTS PARC_GERAL (
    ID_CONTR INT NOT NULL DEFAULT 0,
    NUM_PARC INT NOT NULL,
    NUM_CONTRATO VARCHAR(100) NOT NULL DEFAULT '',
    ID_ACORDO INT NOT NULL DEFAULT 0,
    VENC DATE DEFAULT NULL,
    VALOR DECIMAL(10,2) DEFAULT NULL,
    VALOR_CHEIO DECIMAL(10,2) DEFAULT NULL,
    VALOR_ORIGINAL DECIMAL(10,2) DEFAULT NULL,
    STATUS TINYINT UNSIGNED NOT NULL DEFAULT 1,
    DATA_CARGA DATE DEFAULT NULL,
    DATA_SAIDA_EFET DATE DEFAULT NULL,
    ORIGEM TINYINT NOT NULL DEFAULT 0,
    DATA_IMPORTACAO DATE DEFAULT NULL,
    DATA_BATIM DATETIME DEFAULT NULL,
    DEVOLVIDO TINYINT UNSIGNED NOT NULL DEFAULT 0,
    DATA_SAIDA DATE DEFAULT NULL,
    DATA_ALT DATETIME DEFAULT NULL,
    PRIMARY KEY (ID_CONTR, NUM_PARC, NUM_CONTRATO)
) ENGINE=DUCKDB;

CREATE TABLE IF NOT EXISTS PARC_FINAN (
    ID_CONTR INT NOT NULL DEFAULT 0,
    NUM_PARC DECIMAL(10,3) NOT NULL DEFAULT 0.000,
    ID_NOTIFICACAO INT NOT NULL DEFAULT 0,
    ID_REGUA INT NOT NULL DEFAULT 0,
    ID_ACORDO INT NOT NULL DEFAULT 0,
    VENC DATE DEFAULT NULL,
    VALOR DECIMAL(10,2) DEFAULT NULL,
    VALOR_ORIGINAL DECIMAL(10,2) DEFAULT NULL,
    STATUS TINYINT UNSIGNED NOT NULL DEFAULT 1,
    DATA_CARGA DATE DEFAULT NULL,
    DATA_CAD_MANUAL DATE DEFAULT NULL,
    DATA_SAIDA_EFET DATE DEFAULT NULL,
    DESPESA DECIMAL(10,2) NOT NULL DEFAULT 0.00,
    ORIGEM TINYINT UNSIGNED NOT NULL DEFAULT 0,
    REENTRADA TINYINT NOT NULL DEFAULT 0,
    DATA_IMPORTACAO DATE DEFAULT NULL,
    DATA_ALT DATETIME DEFAULT NULL,
    DEVOLVIDO TINYINT UNSIGNED NOT NULL DEFAULT 0,
    DATA_BATIM DATETIME DEFAULT NULL,
    PRIMARY KEY (ID_CONTR, NUM_PARC)
) ENGINE=DUCKDB;

CREATE TABLE IF NOT EXISTS HISTORICO (
    ID_CONTR INT NOT NULL,
    SEQ SMALLINT UNSIGNED NOT NULL,
    ID_FUNCIONARIO INT NOT NULL DEFAULT 0,
    ID_TEL INT NOT NULL DEFAULT 0,
    DATA DATETIME NOT NULL,
    CODIGO SMALLINT UNSIGNED NOT NULL DEFAULT 0,
    MODO CHAR(1) NOT NULL DEFAULT '*',
    PRIMARY KEY (ID_CONTR, SEQ)
) ENGINE=DUCKDB;

CREATE TABLE IF NOT EXISTS HISTORICO_TEXTO (
    ID_CONTR INT NOT NULL,
    SEQ SMALLINT UNSIGNED NOT NULL,
    MENSAGEM TEXT DEFAULT NULL,
    MOTIVO VARCHAR(255) DEFAULT NULL,
    AUTORIZACAO VARCHAR(255) DEFAULT NULL,
    PRIMARY KEY (ID_CONTR, SEQ)
) ENGINE=DUCKDB;

CREATE TABLE IF NOT EXISTS HISTORICO_SISTEMA (
    ID_CONTR INT NOT NULL,
    SEQ SMALLINT UNSIGNED NOT NULL,
    ID_FUNCIONARIO INT NOT NULL DEFAULT 0,
    ID_TEL INT DEFAULT 0,
    DATA DATETIME NOT NULL,
    CODIGO SMALLINT UNSIGNED NOT NULL DEFAULT 0,
    SUB_CODIGO SMALLINT DEFAULT NULL,
    MENSAGEM VARCHAR(2550) DEFAULT NULL,
    PRIMARY KEY (ID_CONTR, SEQ)
) ENGINE=DUCKDB;
